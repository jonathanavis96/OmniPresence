# --- Build output ---
build/
build-*/
out/
cmake-build-*/
*.user

# --- Qt ---
*.qmlc
*.jsc
moc_*.cpp
qrc_*.cpp
ui_*.h

# --- C/C++ ---
*.o
*.obj
*.exe
*.dll
*.pdb
*.ilk
*.lib
*.exp

# --- Java / RuneLite plugin ---
integrations/runelite/**/.gradle/
integrations/runelite/**/build/
*.class

# --- Node / browser extension / vscode ---
node_modules/
*.vsix
integrations/vscode/**/out/
integrations/browser-extension/**/dist/

# --- Discord Social SDK (vendored binaries are large; fetch via setup script) ---
third_party/discord_social_sdk/

# --- Local private config (NEVER commit real rules/URLs) ---
config/omnipresence.json
config/*.local.json
*.local.json

# --- Secrets / env ---
.env
.env.*
*.secret

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Graphify ---
# graph outputs are NOT committed (regenerable; rebuilt by git hooks / scripts/graphify-build).
# See docs/GRAPHIFY.md. Also covered by the global ~/.config/git/ignore, repeated here
# so the repo behaves the same on machines without that global config.
graphify-out/
