# OmniPresence Tests

Unit tests for the OmniPresence activity presence system live in this directory. The rule engine (matching window titles, process names, browser domains) and template engine (variable substitution and placeholder resolution) are the priority targets for test coverage. Run tests via `ctest --test-dir build` after building the project.
